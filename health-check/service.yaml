apiVersion: v1
kind: Service
metadata:
  name: random-shapes-svc
  namespace: health-check
annotations:
         service.beta.kubernetes.io/azure-load-balancer-resource-group: my-lab
         service.beta.kubernetes.io/azure-pip-name: myAKSPublicIP
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
    #nodePort: 30335
  selector:
    app: random-shapes
  type: LoadBalancer
